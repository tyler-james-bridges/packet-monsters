node_modules

# Build outputs. Every capture tool writes its own dist so parallel runs do not
# clobber each other; none of them belong in the repository.
dist
dist-*
.envdist
artifact

# Capture output and agent scratch tooling.
shots
tools/.*
